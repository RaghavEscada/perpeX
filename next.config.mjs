/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.sanity.io',
      'plus.unsplash.com',
      'images.unsplash.com',
      'unsplash.com'
    ],
  },
};

export default nextConfig;
